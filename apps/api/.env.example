# Creado automaticamente por npm run api:setup, sin alterar .env raiz.
NODE_ENV=development
HOST=127.0.0.1
PORT=3001
DATABASE_URL=
REDIS_URL=
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
SWAGGER_ENABLED=true
APP_ENVIRONMENT=development
SITE_RESOLUTION_MODE=configured
# Consumidas por el frontend al compartir este archivo en Docker local. La clave es opcional.
GOOGLE_MAPS_BROWSER_KEY=
GOOGLE_MAPS_MAP_ID=DEMO_MAP_ID
# Solo para el adaptador local de demostración; tickets:setup genera estos valores.
ACCESS_TOKEN_SECRET=
TICKETS_DEMO_PASSWORD=
